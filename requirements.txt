# Core
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
python-dotenv>=1.0.0
mcp>=1.0.0
tabulate>=0.9.0
rich>=13.0.0
requests>=2.31.0

# Chat UI
gradio>=5.0.0

# Test runners
openai>=1.0.0

# RAG (optional — install separately for RAG features)
# chromadb>=0.5.0
# sentence-transformers>=3.0.0
# langchain-text-splitters>=0.3.0
# langchain-core>=0.3.0

# HTTP transport (optional — for --http flag on MCP server)
# uvicorn>=0.29.0
